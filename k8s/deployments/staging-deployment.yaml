apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-deployment
  namespace: staging

spec:
  replicas: 1

  selector:
    matchLabels:
      app: playwright-demo
      env: staging

  template:
    metadata:
      labels:
        app: playwright-demo
        env: staging

    spec:
      containers:
      - name: nginx
        image: nginx:latest
        imagePullPolicy: Always

        ports:
        - containerPort: 80

        envFrom:
        - configMapRef:
            name: nginx-config

        - secretRef:
            name: nginx-secret
