apiVersion: v1
kind: ConfigMap

metadata:
  name: nginx-config
  namespace: staging

data:
  APP_NAME: "Playwright Demo"
  BASE_URL: "http://nginx-service"
  ENVIRONMENT: "Production"
  TIMEOUT: "60000"